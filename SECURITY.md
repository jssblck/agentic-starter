# Security policy

This is a starter repository rather than a hosted product. Replace this file with the derived project's disclosure address before public release.

For template defects that could cause unsafe generated repositories, open a private security advisory in the template's GitHub repository. Do not include credentials, access tokens, database contents, or proprietary source in a public issue.

The sample application has no authentication or authorization and must not be exposed to an untrusted network without adding them.
